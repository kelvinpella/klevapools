## Agent skills

### Issue tracker

This repo tracks issues in **GitHub Issues** using the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

This repo uses **single-context** domain documentation. See `docs/agents/domain.md`.
